# Dependencies for the helper scripts in this directory.
#   pip install -r scripts/requirements.txt
#
# process_screenshots.py needs Pillow (imported inside main(), so a missing
# install shows up only when the script runs).
# export_localization.py uses the standard library only.
#
# Python 3.10 or newer: process_screenshots.py uses PEP 604 unions
# ("str | None") in its annotations.
Pillow==12.3.0
